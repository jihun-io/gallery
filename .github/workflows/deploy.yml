name: 빌드 및 k3s 배포

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-deploy:
    name: 이미지 빌드 및 배포
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v4

      - name: Docker Buildx 설정
        uses: docker/setup-buildx-action@v3

      - name: GHCR 로그인
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 메타데이터 추출
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha

      - name: Docker 이미지 빌드 및 푸시
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: k8s manifest 업데이트
        run: |
          echo "🔄 deployment.yaml 이미지 태그 업데이트 중..."

          # sha- 프리픽스 + 짧은 SHA (7자리)
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          sed -i "s|image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:.*|image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${SHORT_SHA}|g" k8s/deployment.yaml

          echo "✅ 변경사항:"
          git diff k8s/deployment.yaml

      - name: GitOps 레포 커밋 및 푸시
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

          git add k8s/deployment.yaml

          if git diff --staged --quiet; then
            echo "📝 변경사항 없음"
          else
            git commit -m "배포: 이미지 업데이트 ${{ github.sha }}"
            git push
            echo "✅ k8s manifest 업데이트 완료"
          fi

      - name: 배포 정보 출력
        run: |
          echo "✅ Docker 이미지 빌드 및 푸시 완료!"
          echo "📦 이미지: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
          echo "🔄 k8s manifest 업데이트 완료!"
          echo "⏳ ArgoCD가 자동으로 배포합니다 (약 3분 소요)"
